/*
Parameters are like empty storage boxes that we can name whatever
*/

function pet(animal) {
    console.log(`I have a ${animal} for a pet.`);
}

pet("cat");

function myName(firstName, lastName) {
    console.log(`My name is ${firstName}, ${lastName}`);
}

myName("Gunjan,Aulakh");